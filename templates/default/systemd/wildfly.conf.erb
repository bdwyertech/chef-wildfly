# The configuration you want to run
WILDFLY_CONFIG=<%= @config %>

# The mode you want to run
WILDFLY_MODE=standalone

# The address to bind to
WILDFLY_BIND=0.0.0.0

<% if Array(@server_opts).any? %>
SERVER_OPTS="
  <% @server_opts.each do |opt| -%>
  <%= opt %> \
  <% end %>
"
<% end %>
#        '-Djboss.default.multicast.address=230.1.2.3',
#        '-Djboss.bind.address.management=0.0.0.0',

# JBOSS_HOME
# JBOSS_BASE_DIR
# JBOSS_LOG_DIR
# JBOSS_CONFIG_DIR
# SERVER_OPTS

# Binding Offset
# jboss.socket.binding.port-offset=0

# Public Interface
# jboss.bind.address=127.0.0.1
# jboss.http.port=9990
# jboss.https.port=8443

# Private Interface
# jboss.bind.address.private=127.0.0.1

# Management Interface
# jboss.bind.address.management=127.0.0.1
# jboss.management.http.port=9990
# jboss.management.https.port=9993

# AJP Port
#jboss.ajp.port=8009


# Unsecure Interface
# jboss.bind.address.unsecure=127.0.0.1

# Multicast
# jboss.default.multicast.address=230.0.0.4
